server:
  port: 8081
spring:
  application:
    name: fraud
  datasource:
    username: sergiocuacor
    password: password
    url: jdbc:postgresql://localhost:5432/fraud # Nombre de la BD a la que nos tenemos que conectar
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true